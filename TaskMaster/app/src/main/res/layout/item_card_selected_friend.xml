<?xml version="1.0" encoding="utf-8"?>
<LinearLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    xmlns:tools="http://schemas.android.com/tools"
    android:layout_width="wrap_content"
    android:layout_height="wrap_content"
    android:gravity="center"
    android:orientation="horizontal"
    android:padding="5dp">

    <com.google.android.material.imageview.ShapeableImageView
        android:id="@+id/iv_selected_friend_image"
        android:layout_width="@dimen/item_selected_friend_image_size"
        android:layout_height="@dimen/item_selected_friend_image_size"
        android:contentDescription="@string/image_contentDescription"
        android:src="@drawable/user"
        android:theme="@style/ImageStyleCornerRounded"
        android:visibility="visible"
        app:civ_border_color="@color/primary_text_color"
        app:civ_border_width="@dimen/item_friend_image_circular_width_size"
        tools:visibility="gone" />

    <ImageView
        android:id="@+id/iv_add_friend"
        android:layout_width="@dimen/item_selected_friend_image_size"
        android:layout_height="@dimen/item_selected_friend_image_size"
        android:contentDescription="@string/image_contentDescription"
        android:src="@drawable/baseline_add_24"
        android:visibility="visible"
        app:civ_border_color="#1562e9"
        app:civ_border_width="2dp"
        tools:visibility="visible" />
</LinearLayout>